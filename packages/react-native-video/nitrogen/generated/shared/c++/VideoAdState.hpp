///
/// VideoAdState.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::video {

  /**
   * An enum which can be represented as a JavaScript union (VideoAdState).
   */
  enum class VideoAdState {
    IDLE      SWIFT_NAME(idle) = 0,
    ACTIVATING      SWIFT_NAME(activating) = 1,
    REQUESTING      SWIFT_NAME(requesting) = 2,
    PLAYING      SWIFT_NAME(playing) = 3,
    CONTENT      SWIFT_NAME(content) = 4,
    FAILED      SWIFT_NAME(failed) = 5,
  } CLOSED_ENUM;

} // namespace margelo::nitro::video

namespace margelo::nitro {

  // C++ VideoAdState <> JS VideoAdState (union)
  template <>
  struct JSIConverter<margelo::nitro::video::VideoAdState> final {
    static inline margelo::nitro::video::VideoAdState fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("idle"): return margelo::nitro::video::VideoAdState::IDLE;
        case hashString("activating"): return margelo::nitro::video::VideoAdState::ACTIVATING;
        case hashString("requesting"): return margelo::nitro::video::VideoAdState::REQUESTING;
        case hashString("playing"): return margelo::nitro::video::VideoAdState::PLAYING;
        case hashString("content"): return margelo::nitro::video::VideoAdState::CONTENT;
        case hashString("failed"): return margelo::nitro::video::VideoAdState::FAILED;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum VideoAdState - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::video::VideoAdState arg) {
      switch (arg) {
        case margelo::nitro::video::VideoAdState::IDLE: return JSIConverter<std::string>::toJSI(runtime, "idle");
        case margelo::nitro::video::VideoAdState::ACTIVATING: return JSIConverter<std::string>::toJSI(runtime, "activating");
        case margelo::nitro::video::VideoAdState::REQUESTING: return JSIConverter<std::string>::toJSI(runtime, "requesting");
        case margelo::nitro::video::VideoAdState::PLAYING: return JSIConverter<std::string>::toJSI(runtime, "playing");
        case margelo::nitro::video::VideoAdState::CONTENT: return JSIConverter<std::string>::toJSI(runtime, "content");
        case margelo::nitro::video::VideoAdState::FAILED: return JSIConverter<std::string>::toJSI(runtime, "failed");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert VideoAdState to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("idle"):
        case hashString("activating"):
        case hashString("requesting"):
        case hashString("playing"):
        case hashString("content"):
        case hashString("failed"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
