#pragma once

// Including SDKDDKVer.h defines the highest available Windows platform.

// If one wishes to build one's application for a previous Windows platform, include WinSDKVer.h and
// set the _WIN32_WINNT macro to the platform one wish to support before including SDKDDKVer.h.

#include <SDKDDKVer.h>
